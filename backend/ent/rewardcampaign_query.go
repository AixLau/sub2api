// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
	"github.com/Wei-Shaw/sub2api/ent/rewardcampaign"
	"github.com/Wei-Shaw/sub2api/ent/rewardcampaignjob"
	"github.com/Wei-Shaw/sub2api/ent/rewardcampaignuserstate"
	"github.com/Wei-Shaw/sub2api/ent/rewardcampaignversion"
	"github.com/Wei-Shaw/sub2api/ent/userrewardgrant"
)

// RewardCampaignQuery is the builder for querying RewardCampaign entities.
type RewardCampaignQuery struct {
	config
	ctx                *QueryContext
	order              []rewardcampaign.OrderOption
	inters             []Interceptor
	predicates         []predicate.RewardCampaign
	withVersions       *RewardCampaignVersionQuery
	withCurrentVersion *RewardCampaignVersionQuery
	withGrants         *UserRewardGrantQuery
	withUserStates     *RewardCampaignUserStateQuery
	withJobs           *RewardCampaignJobQuery
	modifiers          []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RewardCampaignQuery builder.
func (_q *RewardCampaignQuery) Where(ps ...predicate.RewardCampaign) *RewardCampaignQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *RewardCampaignQuery) Limit(limit int) *RewardCampaignQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *RewardCampaignQuery) Offset(offset int) *RewardCampaignQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *RewardCampaignQuery) Unique(unique bool) *RewardCampaignQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *RewardCampaignQuery) Order(o ...rewardcampaign.OrderOption) *RewardCampaignQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryVersions chains the current query on the "versions" edge.
func (_q *RewardCampaignQuery) QueryVersions() *RewardCampaignVersionQuery {
	query := (&RewardCampaignVersionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rewardcampaign.Table, rewardcampaign.FieldID, selector),
			sqlgraph.To(rewardcampaignversion.Table, rewardcampaignversion.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, rewardcampaign.VersionsTable, rewardcampaign.VersionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCurrentVersion chains the current query on the "current_version" edge.
func (_q *RewardCampaignQuery) QueryCurrentVersion() *RewardCampaignVersionQuery {
	query := (&RewardCampaignVersionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rewardcampaign.Table, rewardcampaign.FieldID, selector),
			sqlgraph.To(rewardcampaignversion.Table, rewardcampaignversion.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, rewardcampaign.CurrentVersionTable, rewardcampaign.CurrentVersionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGrants chains the current query on the "grants" edge.
func (_q *RewardCampaignQuery) QueryGrants() *UserRewardGrantQuery {
	query := (&UserRewardGrantClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rewardcampaign.Table, rewardcampaign.FieldID, selector),
			sqlgraph.To(userrewardgrant.Table, userrewardgrant.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, rewardcampaign.GrantsTable, rewardcampaign.GrantsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUserStates chains the current query on the "user_states" edge.
func (_q *RewardCampaignQuery) QueryUserStates() *RewardCampaignUserStateQuery {
	query := (&RewardCampaignUserStateClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rewardcampaign.Table, rewardcampaign.FieldID, selector),
			sqlgraph.To(rewardcampaignuserstate.Table, rewardcampaignuserstate.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, rewardcampaign.UserStatesTable, rewardcampaign.UserStatesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryJobs chains the current query on the "jobs" edge.
func (_q *RewardCampaignQuery) QueryJobs() *RewardCampaignJobQuery {
	query := (&RewardCampaignJobClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rewardcampaign.Table, rewardcampaign.FieldID, selector),
			sqlgraph.To(rewardcampaignjob.Table, rewardcampaignjob.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, rewardcampaign.JobsTable, rewardcampaign.JobsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RewardCampaign entity from the query.
// Returns a *NotFoundError when no RewardCampaign was found.
func (_q *RewardCampaignQuery) First(ctx context.Context) (*RewardCampaign, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{rewardcampaign.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *RewardCampaignQuery) FirstX(ctx context.Context) *RewardCampaign {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RewardCampaign ID from the query.
// Returns a *NotFoundError when no RewardCampaign ID was found.
func (_q *RewardCampaignQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{rewardcampaign.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *RewardCampaignQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RewardCampaign entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RewardCampaign entity is found.
// Returns a *NotFoundError when no RewardCampaign entities are found.
func (_q *RewardCampaignQuery) Only(ctx context.Context) (*RewardCampaign, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{rewardcampaign.Label}
	default:
		return nil, &NotSingularError{rewardcampaign.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *RewardCampaignQuery) OnlyX(ctx context.Context) *RewardCampaign {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RewardCampaign ID in the query.
// Returns a *NotSingularError when more than one RewardCampaign ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *RewardCampaignQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{rewardcampaign.Label}
	default:
		err = &NotSingularError{rewardcampaign.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *RewardCampaignQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RewardCampaigns.
func (_q *RewardCampaignQuery) All(ctx context.Context) ([]*RewardCampaign, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RewardCampaign, *RewardCampaignQuery]()
	return withInterceptors[[]*RewardCampaign](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *RewardCampaignQuery) AllX(ctx context.Context) []*RewardCampaign {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RewardCampaign IDs.
func (_q *RewardCampaignQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(rewardcampaign.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *RewardCampaignQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *RewardCampaignQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*RewardCampaignQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *RewardCampaignQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *RewardCampaignQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *RewardCampaignQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RewardCampaignQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *RewardCampaignQuery) Clone() *RewardCampaignQuery {
	if _q == nil {
		return nil
	}
	return &RewardCampaignQuery{
		config:             _q.config,
		ctx:                _q.ctx.Clone(),
		order:              append([]rewardcampaign.OrderOption{}, _q.order...),
		inters:             append([]Interceptor{}, _q.inters...),
		predicates:         append([]predicate.RewardCampaign{}, _q.predicates...),
		withVersions:       _q.withVersions.Clone(),
		withCurrentVersion: _q.withCurrentVersion.Clone(),
		withGrants:         _q.withGrants.Clone(),
		withUserStates:     _q.withUserStates.Clone(),
		withJobs:           _q.withJobs.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithVersions tells the query-builder to eager-load the nodes that are connected to
// the "versions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RewardCampaignQuery) WithVersions(opts ...func(*RewardCampaignVersionQuery)) *RewardCampaignQuery {
	query := (&RewardCampaignVersionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVersions = query
	return _q
}

// WithCurrentVersion tells the query-builder to eager-load the nodes that are connected to
// the "current_version" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RewardCampaignQuery) WithCurrentVersion(opts ...func(*RewardCampaignVersionQuery)) *RewardCampaignQuery {
	query := (&RewardCampaignVersionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCurrentVersion = query
	return _q
}

// WithGrants tells the query-builder to eager-load the nodes that are connected to
// the "grants" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RewardCampaignQuery) WithGrants(opts ...func(*UserRewardGrantQuery)) *RewardCampaignQuery {
	query := (&UserRewardGrantClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withGrants = query
	return _q
}

// WithUserStates tells the query-builder to eager-load the nodes that are connected to
// the "user_states" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RewardCampaignQuery) WithUserStates(opts ...func(*RewardCampaignUserStateQuery)) *RewardCampaignQuery {
	query := (&RewardCampaignUserStateClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUserStates = query
	return _q
}

// WithJobs tells the query-builder to eager-load the nodes that are connected to
// the "jobs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RewardCampaignQuery) WithJobs(opts ...func(*RewardCampaignJobQuery)) *RewardCampaignQuery {
	query := (&RewardCampaignJobClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withJobs = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RewardCampaign.Query().
//		GroupBy(rewardcampaign.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *RewardCampaignQuery) GroupBy(field string, fields ...string) *RewardCampaignGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RewardCampaignGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = rewardcampaign.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RewardCampaign.Query().
//		Select(rewardcampaign.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *RewardCampaignQuery) Select(fields ...string) *RewardCampaignSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &RewardCampaignSelect{RewardCampaignQuery: _q}
	sbuild.label = rewardcampaign.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RewardCampaignSelect configured with the given aggregations.
func (_q *RewardCampaignQuery) Aggregate(fns ...AggregateFunc) *RewardCampaignSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *RewardCampaignQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !rewardcampaign.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *RewardCampaignQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RewardCampaign, error) {
	var (
		nodes       = []*RewardCampaign{}
		_spec       = _q.querySpec()
		loadedTypes = [5]bool{
			_q.withVersions != nil,
			_q.withCurrentVersion != nil,
			_q.withGrants != nil,
			_q.withUserStates != nil,
			_q.withJobs != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RewardCampaign).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RewardCampaign{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withVersions; query != nil {
		if err := _q.loadVersions(ctx, query, nodes,
			func(n *RewardCampaign) { n.Edges.Versions = []*RewardCampaignVersion{} },
			func(n *RewardCampaign, e *RewardCampaignVersion) { n.Edges.Versions = append(n.Edges.Versions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCurrentVersion; query != nil {
		if err := _q.loadCurrentVersion(ctx, query, nodes, nil,
			func(n *RewardCampaign, e *RewardCampaignVersion) { n.Edges.CurrentVersion = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withGrants; query != nil {
		if err := _q.loadGrants(ctx, query, nodes,
			func(n *RewardCampaign) { n.Edges.Grants = []*UserRewardGrant{} },
			func(n *RewardCampaign, e *UserRewardGrant) { n.Edges.Grants = append(n.Edges.Grants, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUserStates; query != nil {
		if err := _q.loadUserStates(ctx, query, nodes,
			func(n *RewardCampaign) { n.Edges.UserStates = []*RewardCampaignUserState{} },
			func(n *RewardCampaign, e *RewardCampaignUserState) {
				n.Edges.UserStates = append(n.Edges.UserStates, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withJobs; query != nil {
		if err := _q.loadJobs(ctx, query, nodes,
			func(n *RewardCampaign) { n.Edges.Jobs = []*RewardCampaignJob{} },
			func(n *RewardCampaign, e *RewardCampaignJob) { n.Edges.Jobs = append(n.Edges.Jobs, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *RewardCampaignQuery) loadVersions(ctx context.Context, query *RewardCampaignVersionQuery, nodes []*RewardCampaign, init func(*RewardCampaign), assign func(*RewardCampaign, *RewardCampaignVersion)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*RewardCampaign)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(rewardcampaignversion.FieldCampaignID)
	}
	query.Where(predicate.RewardCampaignVersion(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(rewardcampaign.VersionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CampaignID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "campaign_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RewardCampaignQuery) loadCurrentVersion(ctx context.Context, query *RewardCampaignVersionQuery, nodes []*RewardCampaign, init func(*RewardCampaign), assign func(*RewardCampaign, *RewardCampaignVersion)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*RewardCampaign)
	for i := range nodes {
		if nodes[i].CurrentVersionID == nil {
			continue
		}
		fk := *nodes[i].CurrentVersionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rewardcampaignversion.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "current_version_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *RewardCampaignQuery) loadGrants(ctx context.Context, query *UserRewardGrantQuery, nodes []*RewardCampaign, init func(*RewardCampaign), assign func(*RewardCampaign, *UserRewardGrant)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*RewardCampaign)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(userrewardgrant.FieldCampaignID)
	}
	query.Where(predicate.UserRewardGrant(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(rewardcampaign.GrantsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CampaignID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "campaign_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RewardCampaignQuery) loadUserStates(ctx context.Context, query *RewardCampaignUserStateQuery, nodes []*RewardCampaign, init func(*RewardCampaign), assign func(*RewardCampaign, *RewardCampaignUserState)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*RewardCampaign)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(rewardcampaignuserstate.FieldCampaignID)
	}
	query.Where(predicate.RewardCampaignUserState(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(rewardcampaign.UserStatesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CampaignID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "campaign_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RewardCampaignQuery) loadJobs(ctx context.Context, query *RewardCampaignJobQuery, nodes []*RewardCampaign, init func(*RewardCampaign), assign func(*RewardCampaign, *RewardCampaignJob)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*RewardCampaign)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(rewardcampaignjob.FieldCampaignID)
	}
	query.Where(predicate.RewardCampaignJob(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(rewardcampaign.JobsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CampaignID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "campaign_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *RewardCampaignQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *RewardCampaignQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(rewardcampaign.Table, rewardcampaign.Columns, sqlgraph.NewFieldSpec(rewardcampaign.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, rewardcampaign.FieldID)
		for i := range fields {
			if fields[i] != rewardcampaign.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withCurrentVersion != nil {
			_spec.Node.AddColumnOnce(rewardcampaign.FieldCurrentVersionID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *RewardCampaignQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(rewardcampaign.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = rewardcampaign.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *RewardCampaignQuery) ForUpdate(opts ...sql.LockOption) *RewardCampaignQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *RewardCampaignQuery) ForShare(opts ...sql.LockOption) *RewardCampaignQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// RewardCampaignGroupBy is the group-by builder for RewardCampaign entities.
type RewardCampaignGroupBy struct {
	selector
	build *RewardCampaignQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *RewardCampaignGroupBy) Aggregate(fns ...AggregateFunc) *RewardCampaignGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *RewardCampaignGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RewardCampaignQuery, *RewardCampaignGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *RewardCampaignGroupBy) sqlScan(ctx context.Context, root *RewardCampaignQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RewardCampaignSelect is the builder for selecting fields of RewardCampaign entities.
type RewardCampaignSelect struct {
	*RewardCampaignQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *RewardCampaignSelect) Aggregate(fns ...AggregateFunc) *RewardCampaignSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *RewardCampaignSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RewardCampaignQuery, *RewardCampaignSelect](ctx, _s.RewardCampaignQuery, _s, _s.inters, v)
}

func (_s *RewardCampaignSelect) sqlScan(ctx context.Context, root *RewardCampaignQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
