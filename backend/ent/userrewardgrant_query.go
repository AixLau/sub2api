// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
	"github.com/Wei-Shaw/sub2api/ent/redeemcode"
	"github.com/Wei-Shaw/sub2api/ent/rewardcampaign"
	"github.com/Wei-Shaw/sub2api/ent/rewardcampaignjob"
	"github.com/Wei-Shaw/sub2api/ent/rewardcampaignversion"
	"github.com/Wei-Shaw/sub2api/ent/rewardskin"
	"github.com/Wei-Shaw/sub2api/ent/user"
	"github.com/Wei-Shaw/sub2api/ent/userrewardgrant"
)

// UserRewardGrantQuery is the builder for querying UserRewardGrant entities.
type UserRewardGrantQuery struct {
	config
	ctx                 *QueryContext
	order               []userrewardgrant.OrderOption
	inters              []Interceptor
	predicates          []predicate.UserRewardGrant
	withCampaign        *RewardCampaignQuery
	withCampaignVersion *RewardCampaignVersionQuery
	withUser            *UserQuery
	withSkin            *RewardSkinQuery
	withJob             *RewardCampaignJobQuery
	withClaimRecord     *RedeemCodeQuery
	modifiers           []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserRewardGrantQuery builder.
func (_q *UserRewardGrantQuery) Where(ps ...predicate.UserRewardGrant) *UserRewardGrantQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *UserRewardGrantQuery) Limit(limit int) *UserRewardGrantQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *UserRewardGrantQuery) Offset(offset int) *UserRewardGrantQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *UserRewardGrantQuery) Unique(unique bool) *UserRewardGrantQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *UserRewardGrantQuery) Order(o ...userrewardgrant.OrderOption) *UserRewardGrantQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCampaign chains the current query on the "campaign" edge.
func (_q *UserRewardGrantQuery) QueryCampaign() *RewardCampaignQuery {
	query := (&RewardCampaignClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userrewardgrant.Table, userrewardgrant.FieldID, selector),
			sqlgraph.To(rewardcampaign.Table, rewardcampaign.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userrewardgrant.CampaignTable, userrewardgrant.CampaignColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCampaignVersion chains the current query on the "campaign_version" edge.
func (_q *UserRewardGrantQuery) QueryCampaignVersion() *RewardCampaignVersionQuery {
	query := (&RewardCampaignVersionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userrewardgrant.Table, userrewardgrant.FieldID, selector),
			sqlgraph.To(rewardcampaignversion.Table, rewardcampaignversion.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userrewardgrant.CampaignVersionTable, userrewardgrant.CampaignVersionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (_q *UserRewardGrantQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userrewardgrant.Table, userrewardgrant.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userrewardgrant.UserTable, userrewardgrant.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySkin chains the current query on the "skin" edge.
func (_q *UserRewardGrantQuery) QuerySkin() *RewardSkinQuery {
	query := (&RewardSkinClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userrewardgrant.Table, userrewardgrant.FieldID, selector),
			sqlgraph.To(rewardskin.Table, rewardskin.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userrewardgrant.SkinTable, userrewardgrant.SkinColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryJob chains the current query on the "job" edge.
func (_q *UserRewardGrantQuery) QueryJob() *RewardCampaignJobQuery {
	query := (&RewardCampaignJobClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userrewardgrant.Table, userrewardgrant.FieldID, selector),
			sqlgraph.To(rewardcampaignjob.Table, rewardcampaignjob.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userrewardgrant.JobTable, userrewardgrant.JobColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryClaimRecord chains the current query on the "claim_record" edge.
func (_q *UserRewardGrantQuery) QueryClaimRecord() *RedeemCodeQuery {
	query := (&RedeemCodeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userrewardgrant.Table, userrewardgrant.FieldID, selector),
			sqlgraph.To(redeemcode.Table, redeemcode.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, userrewardgrant.ClaimRecordTable, userrewardgrant.ClaimRecordColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserRewardGrant entity from the query.
// Returns a *NotFoundError when no UserRewardGrant was found.
func (_q *UserRewardGrantQuery) First(ctx context.Context) (*UserRewardGrant, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userrewardgrant.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *UserRewardGrantQuery) FirstX(ctx context.Context) *UserRewardGrant {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserRewardGrant ID from the query.
// Returns a *NotFoundError when no UserRewardGrant ID was found.
func (_q *UserRewardGrantQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userrewardgrant.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *UserRewardGrantQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserRewardGrant entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserRewardGrant entity is found.
// Returns a *NotFoundError when no UserRewardGrant entities are found.
func (_q *UserRewardGrantQuery) Only(ctx context.Context) (*UserRewardGrant, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userrewardgrant.Label}
	default:
		return nil, &NotSingularError{userrewardgrant.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *UserRewardGrantQuery) OnlyX(ctx context.Context) *UserRewardGrant {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserRewardGrant ID in the query.
// Returns a *NotSingularError when more than one UserRewardGrant ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *UserRewardGrantQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userrewardgrant.Label}
	default:
		err = &NotSingularError{userrewardgrant.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *UserRewardGrantQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserRewardGrants.
func (_q *UserRewardGrantQuery) All(ctx context.Context) ([]*UserRewardGrant, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserRewardGrant, *UserRewardGrantQuery]()
	return withInterceptors[[]*UserRewardGrant](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *UserRewardGrantQuery) AllX(ctx context.Context) []*UserRewardGrant {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserRewardGrant IDs.
func (_q *UserRewardGrantQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(userrewardgrant.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *UserRewardGrantQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *UserRewardGrantQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*UserRewardGrantQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *UserRewardGrantQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *UserRewardGrantQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *UserRewardGrantQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserRewardGrantQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *UserRewardGrantQuery) Clone() *UserRewardGrantQuery {
	if _q == nil {
		return nil
	}
	return &UserRewardGrantQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]userrewardgrant.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.UserRewardGrant{}, _q.predicates...),
		withCampaign:        _q.withCampaign.Clone(),
		withCampaignVersion: _q.withCampaignVersion.Clone(),
		withUser:            _q.withUser.Clone(),
		withSkin:            _q.withSkin.Clone(),
		withJob:             _q.withJob.Clone(),
		withClaimRecord:     _q.withClaimRecord.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithCampaign tells the query-builder to eager-load the nodes that are connected to
// the "campaign" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *UserRewardGrantQuery) WithCampaign(opts ...func(*RewardCampaignQuery)) *UserRewardGrantQuery {
	query := (&RewardCampaignClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCampaign = query
	return _q
}

// WithCampaignVersion tells the query-builder to eager-load the nodes that are connected to
// the "campaign_version" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *UserRewardGrantQuery) WithCampaignVersion(opts ...func(*RewardCampaignVersionQuery)) *UserRewardGrantQuery {
	query := (&RewardCampaignVersionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCampaignVersion = query
	return _q
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *UserRewardGrantQuery) WithUser(opts ...func(*UserQuery)) *UserRewardGrantQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUser = query
	return _q
}

// WithSkin tells the query-builder to eager-load the nodes that are connected to
// the "skin" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *UserRewardGrantQuery) WithSkin(opts ...func(*RewardSkinQuery)) *UserRewardGrantQuery {
	query := (&RewardSkinClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSkin = query
	return _q
}

// WithJob tells the query-builder to eager-load the nodes that are connected to
// the "job" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *UserRewardGrantQuery) WithJob(opts ...func(*RewardCampaignJobQuery)) *UserRewardGrantQuery {
	query := (&RewardCampaignJobClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withJob = query
	return _q
}

// WithClaimRecord tells the query-builder to eager-load the nodes that are connected to
// the "claim_record" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *UserRewardGrantQuery) WithClaimRecord(opts ...func(*RedeemCodeQuery)) *UserRewardGrantQuery {
	query := (&RedeemCodeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withClaimRecord = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserRewardGrant.Query().
//		GroupBy(userrewardgrant.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *UserRewardGrantQuery) GroupBy(field string, fields ...string) *UserRewardGrantGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserRewardGrantGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = userrewardgrant.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.UserRewardGrant.Query().
//		Select(userrewardgrant.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *UserRewardGrantQuery) Select(fields ...string) *UserRewardGrantSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &UserRewardGrantSelect{UserRewardGrantQuery: _q}
	sbuild.label = userrewardgrant.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserRewardGrantSelect configured with the given aggregations.
func (_q *UserRewardGrantQuery) Aggregate(fns ...AggregateFunc) *UserRewardGrantSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *UserRewardGrantQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !userrewardgrant.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *UserRewardGrantQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserRewardGrant, error) {
	var (
		nodes       = []*UserRewardGrant{}
		_spec       = _q.querySpec()
		loadedTypes = [6]bool{
			_q.withCampaign != nil,
			_q.withCampaignVersion != nil,
			_q.withUser != nil,
			_q.withSkin != nil,
			_q.withJob != nil,
			_q.withClaimRecord != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserRewardGrant).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserRewardGrant{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCampaign; query != nil {
		if err := _q.loadCampaign(ctx, query, nodes, nil,
			func(n *UserRewardGrant, e *RewardCampaign) { n.Edges.Campaign = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCampaignVersion; query != nil {
		if err := _q.loadCampaignVersion(ctx, query, nodes, nil,
			func(n *UserRewardGrant, e *RewardCampaignVersion) { n.Edges.CampaignVersion = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUser; query != nil {
		if err := _q.loadUser(ctx, query, nodes, nil,
			func(n *UserRewardGrant, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSkin; query != nil {
		if err := _q.loadSkin(ctx, query, nodes, nil,
			func(n *UserRewardGrant, e *RewardSkin) { n.Edges.Skin = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withJob; query != nil {
		if err := _q.loadJob(ctx, query, nodes, nil,
			func(n *UserRewardGrant, e *RewardCampaignJob) { n.Edges.Job = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withClaimRecord; query != nil {
		if err := _q.loadClaimRecord(ctx, query, nodes, nil,
			func(n *UserRewardGrant, e *RedeemCode) { n.Edges.ClaimRecord = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *UserRewardGrantQuery) loadCampaign(ctx context.Context, query *RewardCampaignQuery, nodes []*UserRewardGrant, init func(*UserRewardGrant), assign func(*UserRewardGrant, *RewardCampaign)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*UserRewardGrant)
	for i := range nodes {
		fk := nodes[i].CampaignID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rewardcampaign.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "campaign_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *UserRewardGrantQuery) loadCampaignVersion(ctx context.Context, query *RewardCampaignVersionQuery, nodes []*UserRewardGrant, init func(*UserRewardGrant), assign func(*UserRewardGrant, *RewardCampaignVersion)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*UserRewardGrant)
	for i := range nodes {
		fk := nodes[i].CampaignVersionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rewardcampaignversion.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "campaign_version_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *UserRewardGrantQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*UserRewardGrant, init func(*UserRewardGrant), assign func(*UserRewardGrant, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*UserRewardGrant)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *UserRewardGrantQuery) loadSkin(ctx context.Context, query *RewardSkinQuery, nodes []*UserRewardGrant, init func(*UserRewardGrant), assign func(*UserRewardGrant, *RewardSkin)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*UserRewardGrant)
	for i := range nodes {
		if nodes[i].SkinID == nil {
			continue
		}
		fk := *nodes[i].SkinID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rewardskin.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "skin_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *UserRewardGrantQuery) loadJob(ctx context.Context, query *RewardCampaignJobQuery, nodes []*UserRewardGrant, init func(*UserRewardGrant), assign func(*UserRewardGrant, *RewardCampaignJob)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*UserRewardGrant)
	for i := range nodes {
		if nodes[i].JobID == nil {
			continue
		}
		fk := *nodes[i].JobID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rewardcampaignjob.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "job_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *UserRewardGrantQuery) loadClaimRecord(ctx context.Context, query *RedeemCodeQuery, nodes []*UserRewardGrant, init func(*UserRewardGrant), assign func(*UserRewardGrant, *RedeemCode)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*UserRewardGrant)
	for i := range nodes {
		if nodes[i].ClaimRecordID == nil {
			continue
		}
		fk := *nodes[i].ClaimRecordID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(redeemcode.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "claim_record_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *UserRewardGrantQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *UserRewardGrantQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userrewardgrant.Table, userrewardgrant.Columns, sqlgraph.NewFieldSpec(userrewardgrant.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userrewardgrant.FieldID)
		for i := range fields {
			if fields[i] != userrewardgrant.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withCampaign != nil {
			_spec.Node.AddColumnOnce(userrewardgrant.FieldCampaignID)
		}
		if _q.withCampaignVersion != nil {
			_spec.Node.AddColumnOnce(userrewardgrant.FieldCampaignVersionID)
		}
		if _q.withUser != nil {
			_spec.Node.AddColumnOnce(userrewardgrant.FieldUserID)
		}
		if _q.withSkin != nil {
			_spec.Node.AddColumnOnce(userrewardgrant.FieldSkinID)
		}
		if _q.withJob != nil {
			_spec.Node.AddColumnOnce(userrewardgrant.FieldJobID)
		}
		if _q.withClaimRecord != nil {
			_spec.Node.AddColumnOnce(userrewardgrant.FieldClaimRecordID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *UserRewardGrantQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(userrewardgrant.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = userrewardgrant.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *UserRewardGrantQuery) ForUpdate(opts ...sql.LockOption) *UserRewardGrantQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *UserRewardGrantQuery) ForShare(opts ...sql.LockOption) *UserRewardGrantQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// UserRewardGrantGroupBy is the group-by builder for UserRewardGrant entities.
type UserRewardGrantGroupBy struct {
	selector
	build *UserRewardGrantQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *UserRewardGrantGroupBy) Aggregate(fns ...AggregateFunc) *UserRewardGrantGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *UserRewardGrantGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserRewardGrantQuery, *UserRewardGrantGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *UserRewardGrantGroupBy) sqlScan(ctx context.Context, root *UserRewardGrantQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserRewardGrantSelect is the builder for selecting fields of UserRewardGrant entities.
type UserRewardGrantSelect struct {
	*UserRewardGrantQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *UserRewardGrantSelect) Aggregate(fns ...AggregateFunc) *UserRewardGrantSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *UserRewardGrantSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserRewardGrantQuery, *UserRewardGrantSelect](ctx, _s.UserRewardGrantQuery, _s, _s.inters, v)
}

func (_s *UserRewardGrantSelect) sqlScan(ctx context.Context, root *UserRewardGrantQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
