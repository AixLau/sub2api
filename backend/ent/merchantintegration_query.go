// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/merchantapiendpoint"
	"github.com/Wei-Shaw/sub2api/ent/merchantbinding"
	"github.com/Wei-Shaw/sub2api/ent/merchantintegration"
	"github.com/Wei-Shaw/sub2api/ent/merchantrechargerecord"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
)

// MerchantIntegrationQuery is the builder for querying MerchantIntegration entities.
type MerchantIntegrationQuery struct {
	config
	ctx                 *QueryContext
	order               []merchantintegration.OrderOption
	inters              []Interceptor
	predicates          []predicate.MerchantIntegration
	withEndpoints       *MerchantAPIEndpointQuery
	withBindings        *MerchantBindingQuery
	withRechargeRecords *MerchantRechargeRecordQuery
	modifiers           []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MerchantIntegrationQuery builder.
func (_q *MerchantIntegrationQuery) Where(ps ...predicate.MerchantIntegration) *MerchantIntegrationQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *MerchantIntegrationQuery) Limit(limit int) *MerchantIntegrationQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *MerchantIntegrationQuery) Offset(offset int) *MerchantIntegrationQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *MerchantIntegrationQuery) Unique(unique bool) *MerchantIntegrationQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *MerchantIntegrationQuery) Order(o ...merchantintegration.OrderOption) *MerchantIntegrationQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryEndpoints chains the current query on the "endpoints" edge.
func (_q *MerchantIntegrationQuery) QueryEndpoints() *MerchantAPIEndpointQuery {
	query := (&MerchantAPIEndpointClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(merchantintegration.Table, merchantintegration.FieldID, selector),
			sqlgraph.To(merchantapiendpoint.Table, merchantapiendpoint.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, merchantintegration.EndpointsTable, merchantintegration.EndpointsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBindings chains the current query on the "bindings" edge.
func (_q *MerchantIntegrationQuery) QueryBindings() *MerchantBindingQuery {
	query := (&MerchantBindingClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(merchantintegration.Table, merchantintegration.FieldID, selector),
			sqlgraph.To(merchantbinding.Table, merchantbinding.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, merchantintegration.BindingsTable, merchantintegration.BindingsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRechargeRecords chains the current query on the "recharge_records" edge.
func (_q *MerchantIntegrationQuery) QueryRechargeRecords() *MerchantRechargeRecordQuery {
	query := (&MerchantRechargeRecordClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(merchantintegration.Table, merchantintegration.FieldID, selector),
			sqlgraph.To(merchantrechargerecord.Table, merchantrechargerecord.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, merchantintegration.RechargeRecordsTable, merchantintegration.RechargeRecordsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MerchantIntegration entity from the query.
// Returns a *NotFoundError when no MerchantIntegration was found.
func (_q *MerchantIntegrationQuery) First(ctx context.Context) (*MerchantIntegration, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{merchantintegration.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *MerchantIntegrationQuery) FirstX(ctx context.Context) *MerchantIntegration {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MerchantIntegration ID from the query.
// Returns a *NotFoundError when no MerchantIntegration ID was found.
func (_q *MerchantIntegrationQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{merchantintegration.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *MerchantIntegrationQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MerchantIntegration entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MerchantIntegration entity is found.
// Returns a *NotFoundError when no MerchantIntegration entities are found.
func (_q *MerchantIntegrationQuery) Only(ctx context.Context) (*MerchantIntegration, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{merchantintegration.Label}
	default:
		return nil, &NotSingularError{merchantintegration.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *MerchantIntegrationQuery) OnlyX(ctx context.Context) *MerchantIntegration {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MerchantIntegration ID in the query.
// Returns a *NotSingularError when more than one MerchantIntegration ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *MerchantIntegrationQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{merchantintegration.Label}
	default:
		err = &NotSingularError{merchantintegration.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *MerchantIntegrationQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MerchantIntegrations.
func (_q *MerchantIntegrationQuery) All(ctx context.Context) ([]*MerchantIntegration, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MerchantIntegration, *MerchantIntegrationQuery]()
	return withInterceptors[[]*MerchantIntegration](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *MerchantIntegrationQuery) AllX(ctx context.Context) []*MerchantIntegration {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MerchantIntegration IDs.
func (_q *MerchantIntegrationQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(merchantintegration.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *MerchantIntegrationQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *MerchantIntegrationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*MerchantIntegrationQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *MerchantIntegrationQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *MerchantIntegrationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *MerchantIntegrationQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MerchantIntegrationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *MerchantIntegrationQuery) Clone() *MerchantIntegrationQuery {
	if _q == nil {
		return nil
	}
	return &MerchantIntegrationQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]merchantintegration.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.MerchantIntegration{}, _q.predicates...),
		withEndpoints:       _q.withEndpoints.Clone(),
		withBindings:        _q.withBindings.Clone(),
		withRechargeRecords: _q.withRechargeRecords.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithEndpoints tells the query-builder to eager-load the nodes that are connected to
// the "endpoints" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MerchantIntegrationQuery) WithEndpoints(opts ...func(*MerchantAPIEndpointQuery)) *MerchantIntegrationQuery {
	query := (&MerchantAPIEndpointClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEndpoints = query
	return _q
}

// WithBindings tells the query-builder to eager-load the nodes that are connected to
// the "bindings" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MerchantIntegrationQuery) WithBindings(opts ...func(*MerchantBindingQuery)) *MerchantIntegrationQuery {
	query := (&MerchantBindingClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBindings = query
	return _q
}

// WithRechargeRecords tells the query-builder to eager-load the nodes that are connected to
// the "recharge_records" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MerchantIntegrationQuery) WithRechargeRecords(opts ...func(*MerchantRechargeRecordQuery)) *MerchantIntegrationQuery {
	query := (&MerchantRechargeRecordClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRechargeRecords = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MerchantIntegration.Query().
//		GroupBy(merchantintegration.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *MerchantIntegrationQuery) GroupBy(field string, fields ...string) *MerchantIntegrationGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MerchantIntegrationGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = merchantintegration.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.MerchantIntegration.Query().
//		Select(merchantintegration.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *MerchantIntegrationQuery) Select(fields ...string) *MerchantIntegrationSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &MerchantIntegrationSelect{MerchantIntegrationQuery: _q}
	sbuild.label = merchantintegration.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MerchantIntegrationSelect configured with the given aggregations.
func (_q *MerchantIntegrationQuery) Aggregate(fns ...AggregateFunc) *MerchantIntegrationSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *MerchantIntegrationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !merchantintegration.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *MerchantIntegrationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MerchantIntegration, error) {
	var (
		nodes       = []*MerchantIntegration{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withEndpoints != nil,
			_q.withBindings != nil,
			_q.withRechargeRecords != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MerchantIntegration).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MerchantIntegration{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withEndpoints; query != nil {
		if err := _q.loadEndpoints(ctx, query, nodes,
			func(n *MerchantIntegration) { n.Edges.Endpoints = []*MerchantAPIEndpoint{} },
			func(n *MerchantIntegration, e *MerchantAPIEndpoint) { n.Edges.Endpoints = append(n.Edges.Endpoints, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withBindings; query != nil {
		if err := _q.loadBindings(ctx, query, nodes,
			func(n *MerchantIntegration) { n.Edges.Bindings = []*MerchantBinding{} },
			func(n *MerchantIntegration, e *MerchantBinding) { n.Edges.Bindings = append(n.Edges.Bindings, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRechargeRecords; query != nil {
		if err := _q.loadRechargeRecords(ctx, query, nodes,
			func(n *MerchantIntegration) { n.Edges.RechargeRecords = []*MerchantRechargeRecord{} },
			func(n *MerchantIntegration, e *MerchantRechargeRecord) {
				n.Edges.RechargeRecords = append(n.Edges.RechargeRecords, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *MerchantIntegrationQuery) loadEndpoints(ctx context.Context, query *MerchantAPIEndpointQuery, nodes []*MerchantIntegration, init func(*MerchantIntegration), assign func(*MerchantIntegration, *MerchantAPIEndpoint)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*MerchantIntegration)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(merchantapiendpoint.FieldIntegrationID)
	}
	query.Where(predicate.MerchantAPIEndpoint(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(merchantintegration.EndpointsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.IntegrationID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "integration_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *MerchantIntegrationQuery) loadBindings(ctx context.Context, query *MerchantBindingQuery, nodes []*MerchantIntegration, init func(*MerchantIntegration), assign func(*MerchantIntegration, *MerchantBinding)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*MerchantIntegration)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(merchantbinding.FieldIntegrationID)
	}
	query.Where(predicate.MerchantBinding(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(merchantintegration.BindingsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.IntegrationID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "integration_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *MerchantIntegrationQuery) loadRechargeRecords(ctx context.Context, query *MerchantRechargeRecordQuery, nodes []*MerchantIntegration, init func(*MerchantIntegration), assign func(*MerchantIntegration, *MerchantRechargeRecord)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*MerchantIntegration)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(merchantrechargerecord.FieldIntegrationID)
	}
	query.Where(predicate.MerchantRechargeRecord(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(merchantintegration.RechargeRecordsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.IntegrationID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "integration_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *MerchantIntegrationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *MerchantIntegrationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(merchantintegration.Table, merchantintegration.Columns, sqlgraph.NewFieldSpec(merchantintegration.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, merchantintegration.FieldID)
		for i := range fields {
			if fields[i] != merchantintegration.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *MerchantIntegrationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(merchantintegration.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = merchantintegration.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *MerchantIntegrationQuery) ForUpdate(opts ...sql.LockOption) *MerchantIntegrationQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *MerchantIntegrationQuery) ForShare(opts ...sql.LockOption) *MerchantIntegrationQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// MerchantIntegrationGroupBy is the group-by builder for MerchantIntegration entities.
type MerchantIntegrationGroupBy struct {
	selector
	build *MerchantIntegrationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *MerchantIntegrationGroupBy) Aggregate(fns ...AggregateFunc) *MerchantIntegrationGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *MerchantIntegrationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MerchantIntegrationQuery, *MerchantIntegrationGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *MerchantIntegrationGroupBy) sqlScan(ctx context.Context, root *MerchantIntegrationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MerchantIntegrationSelect is the builder for selecting fields of MerchantIntegration entities.
type MerchantIntegrationSelect struct {
	*MerchantIntegrationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *MerchantIntegrationSelect) Aggregate(fns ...AggregateFunc) *MerchantIntegrationSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *MerchantIntegrationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MerchantIntegrationQuery, *MerchantIntegrationSelect](ctx, _s.MerchantIntegrationQuery, _s, _s.inters, v)
}

func (_s *MerchantIntegrationSelect) sqlScan(ctx context.Context, root *MerchantIntegrationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
