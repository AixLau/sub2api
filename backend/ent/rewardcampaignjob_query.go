// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
	"github.com/Wei-Shaw/sub2api/ent/rewardcampaign"
	"github.com/Wei-Shaw/sub2api/ent/rewardcampaignjob"
	"github.com/Wei-Shaw/sub2api/ent/rewardcampaignversion"
	"github.com/Wei-Shaw/sub2api/ent/userrewardgrant"
)

// RewardCampaignJobQuery is the builder for querying RewardCampaignJob entities.
type RewardCampaignJobQuery struct {
	config
	ctx                 *QueryContext
	order               []rewardcampaignjob.OrderOption
	inters              []Interceptor
	predicates          []predicate.RewardCampaignJob
	withCampaign        *RewardCampaignQuery
	withCampaignVersion *RewardCampaignVersionQuery
	withGrants          *UserRewardGrantQuery
	modifiers           []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RewardCampaignJobQuery builder.
func (_q *RewardCampaignJobQuery) Where(ps ...predicate.RewardCampaignJob) *RewardCampaignJobQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *RewardCampaignJobQuery) Limit(limit int) *RewardCampaignJobQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *RewardCampaignJobQuery) Offset(offset int) *RewardCampaignJobQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *RewardCampaignJobQuery) Unique(unique bool) *RewardCampaignJobQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *RewardCampaignJobQuery) Order(o ...rewardcampaignjob.OrderOption) *RewardCampaignJobQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCampaign chains the current query on the "campaign" edge.
func (_q *RewardCampaignJobQuery) QueryCampaign() *RewardCampaignQuery {
	query := (&RewardCampaignClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rewardcampaignjob.Table, rewardcampaignjob.FieldID, selector),
			sqlgraph.To(rewardcampaign.Table, rewardcampaign.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, rewardcampaignjob.CampaignTable, rewardcampaignjob.CampaignColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCampaignVersion chains the current query on the "campaign_version" edge.
func (_q *RewardCampaignJobQuery) QueryCampaignVersion() *RewardCampaignVersionQuery {
	query := (&RewardCampaignVersionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rewardcampaignjob.Table, rewardcampaignjob.FieldID, selector),
			sqlgraph.To(rewardcampaignversion.Table, rewardcampaignversion.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, rewardcampaignjob.CampaignVersionTable, rewardcampaignjob.CampaignVersionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGrants chains the current query on the "grants" edge.
func (_q *RewardCampaignJobQuery) QueryGrants() *UserRewardGrantQuery {
	query := (&UserRewardGrantClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rewardcampaignjob.Table, rewardcampaignjob.FieldID, selector),
			sqlgraph.To(userrewardgrant.Table, userrewardgrant.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, rewardcampaignjob.GrantsTable, rewardcampaignjob.GrantsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RewardCampaignJob entity from the query.
// Returns a *NotFoundError when no RewardCampaignJob was found.
func (_q *RewardCampaignJobQuery) First(ctx context.Context) (*RewardCampaignJob, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{rewardcampaignjob.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *RewardCampaignJobQuery) FirstX(ctx context.Context) *RewardCampaignJob {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RewardCampaignJob ID from the query.
// Returns a *NotFoundError when no RewardCampaignJob ID was found.
func (_q *RewardCampaignJobQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{rewardcampaignjob.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *RewardCampaignJobQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RewardCampaignJob entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RewardCampaignJob entity is found.
// Returns a *NotFoundError when no RewardCampaignJob entities are found.
func (_q *RewardCampaignJobQuery) Only(ctx context.Context) (*RewardCampaignJob, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{rewardcampaignjob.Label}
	default:
		return nil, &NotSingularError{rewardcampaignjob.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *RewardCampaignJobQuery) OnlyX(ctx context.Context) *RewardCampaignJob {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RewardCampaignJob ID in the query.
// Returns a *NotSingularError when more than one RewardCampaignJob ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *RewardCampaignJobQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{rewardcampaignjob.Label}
	default:
		err = &NotSingularError{rewardcampaignjob.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *RewardCampaignJobQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RewardCampaignJobs.
func (_q *RewardCampaignJobQuery) All(ctx context.Context) ([]*RewardCampaignJob, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RewardCampaignJob, *RewardCampaignJobQuery]()
	return withInterceptors[[]*RewardCampaignJob](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *RewardCampaignJobQuery) AllX(ctx context.Context) []*RewardCampaignJob {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RewardCampaignJob IDs.
func (_q *RewardCampaignJobQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(rewardcampaignjob.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *RewardCampaignJobQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *RewardCampaignJobQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*RewardCampaignJobQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *RewardCampaignJobQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *RewardCampaignJobQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *RewardCampaignJobQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RewardCampaignJobQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *RewardCampaignJobQuery) Clone() *RewardCampaignJobQuery {
	if _q == nil {
		return nil
	}
	return &RewardCampaignJobQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]rewardcampaignjob.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.RewardCampaignJob{}, _q.predicates...),
		withCampaign:        _q.withCampaign.Clone(),
		withCampaignVersion: _q.withCampaignVersion.Clone(),
		withGrants:          _q.withGrants.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithCampaign tells the query-builder to eager-load the nodes that are connected to
// the "campaign" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RewardCampaignJobQuery) WithCampaign(opts ...func(*RewardCampaignQuery)) *RewardCampaignJobQuery {
	query := (&RewardCampaignClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCampaign = query
	return _q
}

// WithCampaignVersion tells the query-builder to eager-load the nodes that are connected to
// the "campaign_version" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RewardCampaignJobQuery) WithCampaignVersion(opts ...func(*RewardCampaignVersionQuery)) *RewardCampaignJobQuery {
	query := (&RewardCampaignVersionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCampaignVersion = query
	return _q
}

// WithGrants tells the query-builder to eager-load the nodes that are connected to
// the "grants" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RewardCampaignJobQuery) WithGrants(opts ...func(*UserRewardGrantQuery)) *RewardCampaignJobQuery {
	query := (&UserRewardGrantClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withGrants = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RewardCampaignJob.Query().
//		GroupBy(rewardcampaignjob.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *RewardCampaignJobQuery) GroupBy(field string, fields ...string) *RewardCampaignJobGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RewardCampaignJobGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = rewardcampaignjob.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RewardCampaignJob.Query().
//		Select(rewardcampaignjob.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *RewardCampaignJobQuery) Select(fields ...string) *RewardCampaignJobSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &RewardCampaignJobSelect{RewardCampaignJobQuery: _q}
	sbuild.label = rewardcampaignjob.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RewardCampaignJobSelect configured with the given aggregations.
func (_q *RewardCampaignJobQuery) Aggregate(fns ...AggregateFunc) *RewardCampaignJobSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *RewardCampaignJobQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !rewardcampaignjob.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *RewardCampaignJobQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RewardCampaignJob, error) {
	var (
		nodes       = []*RewardCampaignJob{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withCampaign != nil,
			_q.withCampaignVersion != nil,
			_q.withGrants != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RewardCampaignJob).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RewardCampaignJob{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCampaign; query != nil {
		if err := _q.loadCampaign(ctx, query, nodes, nil,
			func(n *RewardCampaignJob, e *RewardCampaign) { n.Edges.Campaign = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCampaignVersion; query != nil {
		if err := _q.loadCampaignVersion(ctx, query, nodes, nil,
			func(n *RewardCampaignJob, e *RewardCampaignVersion) { n.Edges.CampaignVersion = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withGrants; query != nil {
		if err := _q.loadGrants(ctx, query, nodes,
			func(n *RewardCampaignJob) { n.Edges.Grants = []*UserRewardGrant{} },
			func(n *RewardCampaignJob, e *UserRewardGrant) { n.Edges.Grants = append(n.Edges.Grants, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *RewardCampaignJobQuery) loadCampaign(ctx context.Context, query *RewardCampaignQuery, nodes []*RewardCampaignJob, init func(*RewardCampaignJob), assign func(*RewardCampaignJob, *RewardCampaign)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*RewardCampaignJob)
	for i := range nodes {
		fk := nodes[i].CampaignID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rewardcampaign.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "campaign_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *RewardCampaignJobQuery) loadCampaignVersion(ctx context.Context, query *RewardCampaignVersionQuery, nodes []*RewardCampaignJob, init func(*RewardCampaignJob), assign func(*RewardCampaignJob, *RewardCampaignVersion)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*RewardCampaignJob)
	for i := range nodes {
		fk := nodes[i].CampaignVersionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rewardcampaignversion.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "campaign_version_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *RewardCampaignJobQuery) loadGrants(ctx context.Context, query *UserRewardGrantQuery, nodes []*RewardCampaignJob, init func(*RewardCampaignJob), assign func(*RewardCampaignJob, *UserRewardGrant)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*RewardCampaignJob)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(userrewardgrant.FieldJobID)
	}
	query.Where(predicate.UserRewardGrant(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(rewardcampaignjob.GrantsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.JobID
		if fk == nil {
			return fmt.Errorf(`foreign-key "job_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "job_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *RewardCampaignJobQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *RewardCampaignJobQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(rewardcampaignjob.Table, rewardcampaignjob.Columns, sqlgraph.NewFieldSpec(rewardcampaignjob.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, rewardcampaignjob.FieldID)
		for i := range fields {
			if fields[i] != rewardcampaignjob.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withCampaign != nil {
			_spec.Node.AddColumnOnce(rewardcampaignjob.FieldCampaignID)
		}
		if _q.withCampaignVersion != nil {
			_spec.Node.AddColumnOnce(rewardcampaignjob.FieldCampaignVersionID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *RewardCampaignJobQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(rewardcampaignjob.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = rewardcampaignjob.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *RewardCampaignJobQuery) ForUpdate(opts ...sql.LockOption) *RewardCampaignJobQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *RewardCampaignJobQuery) ForShare(opts ...sql.LockOption) *RewardCampaignJobQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// RewardCampaignJobGroupBy is the group-by builder for RewardCampaignJob entities.
type RewardCampaignJobGroupBy struct {
	selector
	build *RewardCampaignJobQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *RewardCampaignJobGroupBy) Aggregate(fns ...AggregateFunc) *RewardCampaignJobGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *RewardCampaignJobGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RewardCampaignJobQuery, *RewardCampaignJobGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *RewardCampaignJobGroupBy) sqlScan(ctx context.Context, root *RewardCampaignJobQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RewardCampaignJobSelect is the builder for selecting fields of RewardCampaignJob entities.
type RewardCampaignJobSelect struct {
	*RewardCampaignJobQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *RewardCampaignJobSelect) Aggregate(fns ...AggregateFunc) *RewardCampaignJobSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *RewardCampaignJobSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RewardCampaignJobQuery, *RewardCampaignJobSelect](ctx, _s.RewardCampaignJobQuery, _s, _s.inters, v)
}

func (_s *RewardCampaignJobSelect) sqlScan(ctx context.Context, root *RewardCampaignJobQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
