# Selected Upstream Integration: v0.2.4

- Upstream tag: `v0.2.4` (`5de5e2bed035d43591a2e10e51f420ef6a84eb98`).
- Scope: selected reliability, platform, account, proxy, monitoring and image
  fixes applied to the local v0.2.3 integration.

## Integrated

- Added MiniMax as a first-class CN provider, including routing, monitoring,
  quota constraints, credentials, UI labels and migration `237`.
- Added HTTP/2 PING health checks for long-running upstream streams.
- Broadcast channel-cache invalidations through Redis for multi-instance setups.
- Preserved persisted account cooldowns and corrected 429 backoff behavior.
- Cancelled upstream streams before closing client-disconnected requests and
  classified those cancellations without failover retries.
- Added Grok media eligibility controls and OpenAI weekly usage estimates.
- Bounded database-backed system logs and kept missing TTFT samples neutral.
- Preserved omitted proxy update fields and directed/repeated expiry fallback.
- Included active subscription groups in the model plaza and upgraded go-redis
  to `v9.22.0`.
- Fixed Claude Code probe handling, Grok unsupported request parameters and
  OAuth Image 2.5 model selection.

## Deliberately excluded

- Upstream sponsor/logo churn, broad documentation redesign and unrelated
  login-page/UI rewrites that conflict with local product behavior.

## Verification

- Backend package compilation passed for service, repository, handler and route
  packages with `go test -run '^$'`.
- Frontend `vue-tsc --noEmit` passed.
- `git diff --check` passed.
