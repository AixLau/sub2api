# Selected Upstream Integration: v0.2.5

- Upstream tag: `v0.2.5` (`86f93c28ee34cc74b629dafb748bd5ac5ca8c5ea`).
- Scope: selected OpenCode integration, site billing mode, batch administration,
  OpenAI image and WebSocket reliability fixes, and related observability and
  quota corrections.
- Method: three-way merge of the upstream delta with local conflict hunks
  retained, followed by migration renumbering and generated-source checks.

## Integrated

- Added OpenCode Zen and Go account types with model-aware protocol routing,
  session cache headers, quota handling, account management and monitoring.
- Added site billing mode controls for recharge and subscription visibility.
- Added bulk subscription operations, API key bulk editing and provider filters.
- Added native OpenAI OAuth image forwarding and the v0.2.5 WebSocket pool
  capacity, execution-scope, retry and reader-loop fixes.
- Added Gemini 3.7/3.8 Flash catalog entries and the upstream request ID and
  usage-detail improvements.
- Added the OpenCode and quota cleanup migrations as `243` and `244` so they
  follow the existing local migration history.

## Selection boundaries

- Local moderation, billing policy, payment providers, public transit and
  product-specific gateway contracts remain the source of truth where upstream
  changed the same integration points.
- Upstream sponsor/logo churn, broad documentation rewrites and OpenSpec design
  artifacts are excluded.

## Verification

- `git diff --check` passed.
- Backend compilation and focused OpenCode, WebSocket, migration, billing and
  route tests were run after integration.
