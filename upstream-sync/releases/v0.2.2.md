# Selected Upstream Integration: v0.2.2

## Boundaries

- Product start: `a6aa8d0191b695655e44ead02c603f43e76f1980`.
- Previous selected upstream: `v0.2.1` / `578785ee7fb35030b094b69624efe25670a36f5f`.
- Target: `v0.2.2` / `5485f368b29d05adb95a00f71801c7c23d8f48af`.
- Method: three-way application of the `v0.2.1..v0.2.2` changes to
  `backend/`, `frontend/`, and `Makefile`, followed by semantic reconciliation.
  This continues the existing selected-integration history; it does not claim
  that the upstream tag is a Git ancestor of the product branch.
- Excluded: upstream README changes and the Codex pinned-account OpenSpec
  design/proposal artifacts. Their functional implementation is included.

## Reconciliation

- Mount model allowlist admission after API-key authentication and before
  composite model rewriting, retaining the local moderation route registrars
  and the root, asynchronous image, and Codex aliases.
- Check the first and subsequent WebSocket turns against the allowlist while
  retaining local per-frame content moderation and per-turn billing.
- Retain local streaming error deduplication: a bare error followed by
  `response.failed` produces one provider event, now classified `stream_failed`.
- Retain local pricing overrides, account statistics, payment providers,
  public transit services, reasoning suffix matching, proxy preselection,
  and exact usage time ranges.
- Regenerate Ent and Wire from the reconciled sources; preserve the local
  admin usage repository and public transit dependencies.
- Reconcile duplicate Astra catalog entries and test declarations introduced
  by earlier selected integration. Adapt new handler tests to local route
  metadata and frontend tests to local stores and component stubs.
- Register the existing public transit locale modules. Permit the deliberately
  empty English purchase-headline prefix in the new locale completeness check.

## Upgrade Behavior

Migration `235_group_model_allowlist.sql` renames `models_list_config` to
`model_allowlist` and preserves its data. An enabled old listing configuration
now rejects requests for models outside that list, including count-token
requests. Review enabled group configurations before deploying.

## Verification

- Ent and Wire generation completed; regenerated Ent output matched the patch.
- Frontend production build and lint passed.
- Backend production build with embedded frontend assets passed.
- Changed frontend regression suite: 20 files, 234 tests passed.
- Locale and usage-page checks: 13 files, 70 tests passed.
- Browser checks with mocked API data: group list and allowlist edit dialog at
  1440x1000 and 390x844, no page errors or document overflow.
- Focused handler, admin, middleware, routing, repository and migration checks
  passed. All `internal/pkg/...` tests passed.
- Isolated upgrade service verification: 593 top-level tests passed (1,247
  passing events including subtests), excluding the three baseline failures
  listed below. The isolated production build also passed.
- Full backend testing is not a clean baseline: pre-existing moderation
  pipeline, continuation, service-tier, usage SQL and settings-contract tests
  were reproduced at `a6aa8d019`. Validation logs and browser captures are kept
  locally under `.git/upstream-sync/v022-*` and `.git/upstream-sync/groups-*`.

The selected service suite has three baseline failures, reproduced unchanged
in the isolated product-start checkout and excluded from its verification run:

- `TestBuildOpenAIWSHeadersNamespacesCodexIdentityByOAuthAccount`
- `TestProxyOpenAIWSHTTPBridgeTurn_UpstreamDefaultServiceTierWinsOverRequest`
- `TestProxyOpenAIWSHTTPBridgeTurn_UpstreamDefaultWinsOverFastAlias`

Concurrent semantic moderation and Spark scheduling changes were committed
separately as `332d619d3` and are preserved as the upgrade's parent. Final
service verification uses an isolated snapshot of the staged upgrade against
the product-start commit, keeping the two changes independently reviewable.

No production deployment or remote push is part of this integration.

## Next Selected Update

`baseline.yaml` requires the upstream commit to be a Git ancestor of the
integration commit. It is therefore not advanced for this selected port.
Use explicit boundaries for the next selected update:

```sh
tools/upstream-sync impact analyze --from v0.2.2 --target <next-upstream-tag> --local-from 64e91cc0c --head HEAD --simulate=false
```
