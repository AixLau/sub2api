# Selected Upstream Integration: v0.2.3

- Upstream tag: `v0.2.3` (`8fa67d477d6651a744754392a8982ea589c26ae6`).
- Scope: apply the `v0.2.2..v0.2.3` functional delta to the current product
  integration, preserving the previously reviewed local gateway and billing
  contracts.

## Integrated

- Added replayable migration `236_group_model_allowlist_repair.sql`. It repairs
  databases where the earlier rename migration was recorded but the
  `model_allowlist` column is absent, backfills when both columns exist, and
  guarantees a non-null JSONB default.
- Added Ollama Cloud DeepSeek output token clamping across Anthropic Messages,
  raw Chat Completions and native Responses paths, including trailing-slash
  base URLs and mapped model names.
- Applied Bearer authentication for Anthropic requests selected to Ollama Cloud
  based on the actual base URL, including count-token and account-test paths.
- Preserved the local account-test model picker fallback and combined it with
  upstream normalization of missing `display_name` and `type` fields.
- Set the application version to `v0.2.3`.

## Selection rationale

The migration and Ollama fixes are upstream correctness fixes and were adopted
directly. The account-test display-name fix was already present locally; the
upstream `type=model` normalization was combined with it. Existing local model
discovery, pricing, payment, moderation, public-transit and gateway identity
contracts remain unchanged.

## Verification

- v0.2.3 focused service, repository and admin tests passed except for the two
  pre-existing usage-log account-test SQL assertions documented in the prior
  integration record.
- The new migration repair, Ollama Cloud clamp/auth, and account model picker
  tests passed.
- `git diff --check` passed and the working tree has no merge conflicts.

No remote push or production deployment was performed.
